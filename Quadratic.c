#include<stdio.h>

void main(){
    float a,b,c,disc,r1,r2,sqrt;
    printf("Enter three co-efficient");
    scanf("%f %f %f",&a,&b,&c);
    if(a==0){
        printf("****Not a quadratic equation****");
    }
    else{
        b*b -4.0 *2*c;
    if(disc ==0){
    printf("complex Root\n");
    disc = disc;
    r1 = -b(2.0*a)
    r2 =sqrt(disc);
    }
}
}