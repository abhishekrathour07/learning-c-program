#include<stdio.h>
#include<string.h>
void main(){
    char st[]= "hellobrother";
    // char *st= "hellobrother"; --> we can write string in this way also.
    int a = strlen(st);
   printf(" The length of a string is %d", a);

} 